rules_version = '2';
// HACKATHON DEMO ONLY — fully open. Anyone can read/write everything.
// Revert to least-privilege rules before production; your API key is public in the client.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
