diff --git a/node_modules/@expo/ngrok/src/client.js b/node_modules/@expo/ngrok/src/client.js
index 3f748a3..35e21c0 100644
--- a/node_modules/@expo/ngrok/src/client.js
+++ b/node_modules/@expo/ngrok/src/client.js
@@ -27,9 +27,16 @@ class NgrokClient {
         return await this.internalApi[method](path, { json: options }).json();
       }
     } catch (error) {
+      const rawBody =
+        error.response && error.response.body != null
+          ? error.response.body
+          : undefined;
+      if (rawBody === undefined) {
+        throw error;
+      }
       let clientError;
       try {
-        const response = JSON.parse(error.response.body);
+        const response = JSON.parse(rawBody);
         clientError = new NgrokClientError(
           response.msg,
           error.response,
@@ -37,9 +44,9 @@ class NgrokClient {
         );
       } catch (e) {
         clientError = new NgrokClientError(
-          error.response.body,
+          String(rawBody),
           error.response,
-          error.response.body
+          rawBody
         );
       }
       throw clientError;
@@ -52,7 +59,14 @@ class NgrokClient {
         (response) => response.statusCode === 204
       );
     } catch (error) {
-      const response = JSON.parse(error.response.body);
+      const rawBody =
+        error.response && error.response.body != null
+          ? error.response.body
+          : undefined;
+      if (rawBody === undefined) {
+        throw error;
+      }
+      const response = JSON.parse(rawBody);
       throw new NgrokClientError(response.msg, error.response, response);
     }
   }
